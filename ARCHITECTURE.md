# 프로젝트 구조 및 아키텍처

## 📁 프로젝트 파일 구조

```
interview-simulator/
├── interview_simulator.py       # 메인 애플리케이션 (Gradio 앱)
├── setup_search_index.py        # Azure AI Search 인덱스 설정
├── requirements.txt             # Python 패키지 의존성
├── Dockerfile                   # Docker 컨테이너 설정
├── deploy.sh                    # Azure Container Apps 배포 스크립트
├── quickstart.sh                # 로컬 개발 환경 빠른 시작
├── .env.template                # 환경 변수 템플릿
├── .env                         # 환경 변수 (비공개, .gitignore에 추가)
├── .gitignore                   # Git 무시 파일
└── README.md                    # 프로젝트 문서
```

---

## 🏛️ 시스템 아키텍처 상세

### 1. 전체 데이터 흐름

```
┌─────────────────────────────────────────────────────────────────────┐
│                          사용자 인터페이스 (Gradio)                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐│
│  │프로필 입력    │  │질문 표시     │  │답변 입력     │  │평가 표시     ││
│  │+ 난이도      │  │+ 음성 재생   │  │+ 음성 녹음   │  │+ 피드백 음성 ││
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘│
└────────────┬────────────────────────────┬────────────────────┬───────┘
             │                            │                    │
             ▼                            ▼                    ▼
┌────────────────────────────┐  ┌──────────────────┐  ┌─────────────────┐
│   질문 생성 파이프라인      │  │  음성 처리       │  │  답변 평가      │
└────────────────────────────┘  └──────────────────┘  └─────────────────┘
             │                            │                    │
    ┌────────┼────────┐          ┌────────┴────────┐  ┌────────┴────────┐
    ▼        ▼        ▼          ▼                 ▼  ▼                 ▼
┌────────┐ ┌────┐ ┌──────┐  ┌────────┐      ┌────────┐ ┌──────────┐ ┌────┐
│AI Search│ │GPT4│ │DALL-E│  │Speech  │      │Speech  │ │  GPT-4   │ │TTS │
│(RAG)   │ │    │ │  3   │  │STT     │      │Custom  │ │(평가)    │ │    │
│        │ │    │ │      │  │        │      │Voice   │ │          │ │    │
└────────┘ └────┘ └──────┘  └────────┘      └────────┘ └──────────┘ └────┘
```

### 2. RAG (Retrieval-Augmented Generation) 파이프라인

```
[사용자 프로필] → [임베딩 생성] → [벡터 검색]
                                      ↓
                               [Top-K 질문 검색]
                                      ↓
                           [관련 질문 + 컨텍스트]
                                      ↓
                    [GPT-4로 맞춤형 질문 생성]
                                      ↓
                    [할루시네이션 최소화된 질문]
```

**RAG의 장점:**
- ✅ 검색된 실제 질문 데이터에 근거
- ✅ 할루시네이션(거짓 정보) 방지
- ✅ 도메인 특화 지식 활용
- ✅ 일관성 있는 평가 기준

### 3. 음성 처리 흐름

#### STT (Speech-to-Text)
```
[사용자 음성] → [마이크 녹음] → [WAV 파일]
                                    ↓
                        [Azure Speech STT]
                                    ↓
                              [텍스트 변환]
                                    ↓
                          [답변 평가 파이프라인]
```

#### TTS (Text-to-Speech with Custom Voice)
```
[생성된 질문/피드백] → [SSML 포맷팅]
                            ↓
                  [Custom Voice 설정]
                            ↓
                  [Azure Speech TTS]
                            ↓
                        [음성 파일]
                            ↓
                    [Gradio 오디오 재생]
```

**Custom Voice 특징:**
- 📢 자신의 목소리로 면접관 역할
- 🎯 자연스러운 억양과 톤
- ⚙️ 속도, 음높이 조절 가능

---

## 🔧 핵심 컴포넌트 설명

### 1. InterviewSimulator 클래스

```python
class InterviewSimulator:
    """면접 시뮬레이션 핵심 로직"""
    
    # 주요 메서드:
    def generate_interview_question()  # RAG 기반 질문 생성
    def speech_to_text()              # 음성 → 텍스트
    def text_to_speech()              # 텍스트 → 음성 (Custom Voice)
    def evaluate_answer()             # 답변 평가
    def search_interview_questions()  # 벡터 검색
    def generate_image()              # DALL-E 이미지 생성
```

### 2. Gradio 인터페이스

**구성 요소:**
- **입력 섹션**: 프로필, 난이도, 시각자료 옵션
- **질문 표시**: 텍스트 + 음성 + 이미지
- **답변 입력**: 음성 녹음 or 텍스트 입력
- **평가 표시**: 점수, 강점, 개선점, 피드백 음성

### 3. Azure AI Search 인덱스

**필드 구조:**
```json
{
  "id": "고유 식별자",
  "question": "면접 질문",
  "category": "기술역량 | 소프트스킬",
  "difficulty": "하 | 중 | 상",
  "context": "질문 의도 및 평가 포인트",
  "sample_answer": "모범 답변 예시",
  "contentVector": [임베딩 벡터 1536차원],
  "tags": ["관련 태그들"]
}
```

**벡터 검색 알고리즘:**
- HNSW (Hierarchical Navigable Small World)
- Cosine 유사도
- Top-K 검색

---

## 🚀 배포 아키텍처

### Azure Container Apps 환경

```
Internet
   │
   ▼
[Azure Front Door]  ← 선택사항: CDN, WAF
   │
   ▼
[Load Balancer]
   │
   ├─────────┬─────────┬─────────┐
   ▼         ▼         ▼         ▼
[Container] [Container] [Container] ...
   │         │         │
   └─────────┴─────────┴─────────┘
              │
              ▼
      [Environment Variables]
              │
   ┌──────────┼──────────┐
   ▼          ▼          ▼
[Speech]  [OpenAI]  [Search]
```

**Auto-scaling 규칙:**
- Min replicas: 1
- Max replicas: 5
- CPU 사용률 > 70% → 스케일 아웃
- 요청 없음 > 5분 → 스케일 인

---

## 🔐 보안 아키텍처

### 1. 인증 흐름 (선택사항)

```
[사용자] → [Azure AD 로그인]
              ↓
         [JWT 토큰 발급]
              ↓
      [Container App 인증]
              ↓
        [애플리케이션]
```

### 2. 비밀 관리

```
[Azure Key Vault]
       │
       ├─ Speech API Key
       ├─ OpenAI API Key
       ├─ Search API Key
       └─ Storage Connection String
              │
              ▼
     [Container Apps Secrets]
              │
              ▼
       [Environment Variables]
```

**설정 방법:**
```bash
# Key Vault에 비밀 추가
az keyvault secret set \
    --vault-name my-keyvault \
    --name speech-key \
    --value "actual-key-value"

# Container App에서 참조
az containerapp update \
    --name interview-simulator \
    --set-env-vars \
    "AZURE_SPEECH_KEY=secretref:speech-key"
```

---

## 📊 모니터링 아키텍처

### Application Insights 통합

```
[Container App]
       │
       ├─ 로그 → [Log Analytics]
       ├─ 메트릭 → [Metrics]
       └─ 트레이스 → [Application Insights]
                          │
              ┌───────────┼───────────┐
              ▼           ▼           ▼
         [대시보드]   [알림]      [분석]
```

**추적 지표:**
- 요청 수 & 응답 시간
- API 호출 횟수 (Speech, OpenAI, Search)
- 오류율 & 예외
- 사용자 세션 길이
- 질문-답변 사이클 시간

---

## 🔄 CI/CD 파이프라인 (선택사항)

### GitHub Actions 예시

```yaml
name: Deploy to Azure

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Build Docker image
        run: docker build -t ${{ secrets.ACR_NAME }}.azurecr.io/interview-simulator:${{ github.sha }} .
      
      - name: Push to ACR
        run: |
          az acr login --name ${{ secrets.ACR_NAME }}
          docker push ${{ secrets.ACR_NAME }}.azurecr.io/interview-simulator:${{ github.sha }}
      
      - name: Deploy to Container Apps
        run: |
          az containerapp update \
            --name interview-simulator \
            --image ${{ secrets.ACR_NAME }}.azurecr.io/interview-simulator:${{ github.sha }}
```

---

## 💡 확장 가능성

### 1. 다중 언어 지원
```python
# 언어별 Custom Voice 추가
CUSTOM_VOICES = {
    'ko': 'ko-KR-MyVoice',
    'en': 'en-US-MyVoiceNeural',
    'ja': 'ja-JP-MyVoiceNeural'
}
```

### 2. 산업별 특화
```python
# 산업별 질문 인덱스
INDEXES = {
    'tech': 'interview-tech-questions',
    'finance': 'interview-finance-questions',
    'consulting': 'interview-consulting-questions'
}
```

### 3. 실시간 협업 면접
- Azure SignalR Service로 WebSocket 연결
- 다중 면접관 시뮬레이션
- 실시간 피드백 공유

### 4. AI 면접 분석 리포트
- 면접 세션 녹화
- 비언어적 요소 분석 (Azure Cognitive Services)
- 종합 분석 리포트 PDF 생성

---

## 📈 성능 최적화

### 1. 캐싱 전략
```python
from functools import lru_cache

@lru_cache(maxsize=100)
def get_embedding(text: str):
    """임베딩 결과 캐싱"""
    return client.embeddings.create(...)
```

### 2. 비동기 처리
```python
import asyncio

async def process_parallel():
    """병렬 API 호출"""
    tasks = [
        generate_question(),
        generate_image(),
        search_context()
    ]
    results = await asyncio.gather(*tasks)
    return results
```

### 3. 연결 풀링
```python
from azure.core.pipeline import Pipeline
from azure.core.pipeline.policies import RetryPolicy

# 재시도 정책 설정
retry_policy = RetryPolicy(
    retry_total=3,
    retry_backoff_factor=0.8
)
```

---

## 🎯 향후 로드맵

1. **Q1 2025**
   - [ ] 모바일 앱 (React Native)
   - [ ] 면접 통계 대시보드
   - [ ] 맞춤형 학습 경로 추천

2. **Q2 2025**
   - [ ] 실시간 코드 테스트 (Codex 통합)
   - [ ] 화상 면접 시뮬레이션
   - [ ] 감정 분석 (표정, 목소리)

3. **Q3 2025**
   - [ ] 멀티플레이어 모드
   - [ ] 기업 맞춤형 질문 DB
   - [ ] VR/AR 면접 체험

---

이 아키텍처는 확장 가능하며, 필요에 따라 각 컴포넌트를 독립적으로 개선할 수 있습니다.
