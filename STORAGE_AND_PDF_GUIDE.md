# 📦 데이터 저장 및 PDF 리포트 가이드

## 🎯 개요

이제 시뮬레이터에서 다음 기능을 사용할 수 있습니다:

1. **Azure Blob Storage**: 질문/답변/평가 데이터 자동 저장
2. **PDF 리포트**: 면접 분석 결과를 PDF로 다운로드

---

## ☁️ Azure Blob Storage 설정

### 1단계: Storage Account 생성

**Azure Portal에서:**
1. "Storage accounts" 검색
2. "+ Create" 클릭
3. 설정:
   - Resource group: 기존 또는 새로 생성
   - Storage account name: `semiconductorstorage` (고유해야 함)
   - Region: Korea Central
   - Performance: Standard
   - Redundancy: LRS (로컬 중복)
4. "Review + create" → "Create"

### 2단계: 연결 문자열 복사

**Storage Account 생성 후:**
1. 생성된 Storage Account로 이동
2. 왼쪽 메뉴에서 "Access keys" 클릭
3. "key1" 아래의 "Connection string" 복사
4. `.env` 파일에 붙여넣기

### 3단계: .env 파일 설정

```bash
# Azure Blob Storage 설정
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=semiconductorstorage;AccountKey=your-key-here;EndpointSuffix=core.windows.net

# 컨테이너 이름 (자동 생성됨)
AZURE_STORAGE_CONTAINER=semiconductor-data
```

### 4단계: 패키지 설치

```bash
pip install azure-storage-blob reportlab
```

---

## 💾 데이터 저장 기능

### 자동 저장되는 데이터

**1. Q&A 레코드**
- 질문 내용
- 학생 답변
- 평가 결과 (점수, 강점, 개선점)
- 타임스탬프

**2. 면접 세션**
- 전체 Q&A 리스트
- 프로필 정보
- 평균 점수
- 세션 메타데이터

### 저장 위치

Blob Storage 구조:
```
semiconductor-data/
├── user_001/
│   ├── qa_records/
│   │   ├── 20241205_143020.json
│   │   ├── 20241205_143145.json
│   │   └── ...
│   └── interview_sessions/
│       ├── 20241205_145030.json
│       └── ...
└── user_002/
    └── ...
```

### 저장 내용 예시

**qa_records/20241205_143020.json:**
```json
{
  "user_id": "user_001",
  "session_type": "qa_records",
  "timestamp": "2024-12-05T14:30:20",
  "data": {
    "question": "CVD 공정에서...",
    "answer": "CVD는...",
    "evaluation": {
      "total_score": 85,
      "scores": {
        "accuracy": 27,
        "depth": 22,
        "structure": 18,
        "application": 13,
        "communication": 9
      },
      "strengths": ["정확한 용어 사용", "체계적 설명"],
      "improvements": ["실무 사례 추가 필요"],
      "recommended_topics": ["PECVD vs LPCVD"]
    }
  }
}
```

---

## 📄 PDF 리포트 생성

### 사용 방법

**면접 모드에서:**

1. 여러 개의 질문에 답변
2. 화면 하단 "📄 면접 결과 리포트" 섹션으로 이동
3. 이름 입력 (예: "홍길동")
4. "📥 PDF 리포트 생성" 클릭
5. 생성된 PDF 다운로드

### PDF 리포트 포함 내용

**페이지 1: 기본 정보 및 종합 평가**
```
🎓 반도체 공정 면접 분석 리포트

📋 기본 정보
- 이름: 홍길동
- 분석 일시: 2024년 12월 5일 14:30
- 총 질문 수: 5개
- 학력: 서울대학교 재료공학부 3학년

📊 종합 평가
┌─────────────┬────────┬──────┬────────┐
│ 평가 항목   │평균점수│ 만점 │ 달성률 │
├─────────────┼────────┼──────┼────────┤
│ 총점        │  82.5  │ 100  │  83%   │
│ 정확성      │  25.2  │  30  │  84%   │
│ 깊이        │  20.5  │  25  │  82%   │
│ 구조        │  16.8  │  20  │  84%   │
│ 응용        │  12.0  │  15  │  80%   │
│ 의사소통    │   8.0  │  10  │  80%   │
└─────────────┴────────┴──────┴────────┘
```

**페이지 2-N: 질문별 상세 분석**
```
📝 질문별 상세 분석

질문 1
CVD 공정에서 온도와 압력이 증착 속도에 미치는 영향을...

답변
CVD는 화학 기상 증착으로...

점수: 85/100

💪 강점:
  • 정확한 용어 사용
  • 체계적인 설명 구조
  • Arrhenius 식 언급

📈 개선점:
  • 실무 사례 추가 필요
  • PECVD와 LPCVD 비교 보완

────────────────────────────────────

질문 2
...
```

**마지막 페이지: 종합 피드백**
```
💡 종합 피드백 및 학습 가이드

✅ 주요 강점:
  • 반도체 공정 이론에 대한 탄탄한 기초
  • 정확한 전문 용어 사용
  • 논리적이고 체계적인 설명
  • 수식과 원리를 연결하는 능력

🎯 중점 개선 사항:
  • 실무 경험 사례 추가 필요
  • 다양한 공정 비교 분석 능력 향상
  • 문제 해결 접근법 구체화

📚 복습 추천 주제:
  • PECVD vs LPCVD 차이점
  • 플라즈마 메커니즘
  • RIE 식각 파라미터 최적화
  • ALD 공정 원리
```

---

## 🛠️ 고급 기능

### 사용자 ID 설정

기본적으로 `user_001`로 저장되지만, 커스터마이징 가능:

**.env 파일에 추가:**
```bash
USER_ID=student_kim_jiwon
```

또는 코드에서:
```python
import os
os.environ['USER_ID'] = 'student_' + user_name.lower().replace(' ', '_')
```

### 저장된 데이터 조회

**Python 스크립트로:**
```python
from storage_manager import storage_manager

# 사용자의 모든 세션 조회
user_id = "user_001"
sessions = storage_manager.get_user_sessions(user_id)
print(f"총 {len(sessions)}개 세션 발견")

# 특정 타입만 조회
qa_records = storage_manager.get_user_sessions(user_id, "qa_records")
print(f"Q&A 레코드: {len(qa_records)}개")
```

**Azure Portal에서:**
1. Storage Account 이동
2. "Containers" 클릭
3. `semiconductor-data` 컨테이너 선택
4. 폴더 구조 탐색
5. JSON 파일 다운로드/확인

---

## 🔒 보안 및 프라이버시

### 저장되는 데이터
- ✅ 질문/답변 텍스트
- ✅ 평가 점수 및 피드백
- ✅ 타임스탬프
- ❌ 개인 식별 정보는 별도 저장 안 됨

### 보안 권장사항
1. **액세스 키 관리**
   - `.env` 파일을 git에 절대 커밋하지 마세요
   - `.gitignore`에 `.env` 추가

2. **SAS 토큰 사용 (선택)**
   - 더 세밀한 권한 제어
   - 시간 제한 액세스

3. **데이터 암호화**
   - Azure Storage는 기본적으로 암호화됨 (AES-256)

---

## 📊 데이터 분석

저장된 데이터로 학습 분석:

```python
import json
from azure.storage.blob import BlobServiceClient

# 연결
connection_string = os.getenv('AZURE_STORAGE_CONNECTION_STRING')
blob_service = BlobServiceClient.from_connection_string(connection_string)
container = blob_service.get_container_client('semiconductor-data')

# 모든 Q&A 레코드 읽기
qa_data = []
blobs = container.list_blobs(name_starts_with='user_001/qa_records/')

for blob in blobs:
    blob_client = container.get_blob_client(blob.name)
    data = json.loads(blob_client.download_blob().readall())
    qa_data.append(data['data'])

# 분석
total_questions = len(qa_data)
avg_score = sum(q['evaluation']['total_score'] for q in qa_data) / total_questions

print(f"총 질문 수: {total_questions}")
print(f"평균 점수: {avg_score:.1f}")

# 약점 분석
weak_topics = {}
for q in qa_data:
    if q['evaluation']['total_score'] < 70:
        for topic in q['evaluation'].get('recommended_topics', []):
            weak_topics[topic] = weak_topics.get(topic, 0) + 1

print("집중 학습 필요 주제:", weak_topics)
```

---

## 💰 비용 예상

### Azure Blob Storage

**무료 티어 (처음 12개월):**
- 5GB LRS 스토리지 무료
- 20,000 읽기 작업 무료
- 2,000 쓰기 작업 무료

**유료 (12개월 후):**
- 스토리지: ~₩25/GB/월
- 작업: 읽기 ₩0.4/만건, 쓰기 ₩5/만건

**예상 사용량 (학생 50명, 월):**
- 데이터: ~100MB (₩2.5)
- 작업: ~10,000건 (₩5)
- **총: ~₩7.5/월**

매우 저렴합니다! 💰

---

## ❓ 문제 해결

### "Blob Storage가 설정되지 않았습니다"
→ 정상입니다. Storage는 선택사항이며, 없어도 시뮬레이터 작동

**설정하려면:**
```bash
# .env 파일 확인
AZURE_STORAGE_CONNECTION_STRING=...

# 패키지 설치
pip install azure-storage-blob
```

### "reportlab 패키지가 설치되지 않았습니다"
```bash
pip install reportlab
```

### "한글 폰트를 찾을 수 없습니다"
PDF는 생성되지만 한글이 제대로 표시 안 될 수 있음

**해결:**
- Windows: 시스템에 이미 있음 (malgun.ttf)
- macOS: AppleGothic 자동 사용
- Linux:
```bash
sudo apt-get install fonts-nanum
```

### PDF 다운로드가 안 됨
1. 먼저 질문에 답변하세요 (최소 1개)
2. "📥 PDF 리포트 생성" 클릭
3. 파일이 생성되면 자동으로 다운로드 링크 표시

---

## 🎯 활용 예시

### 학생 개인
```
1. 주간 학습: 매주 10개 질문 연습
2. PDF 리포트 생성: 주간 학습 결과 확인
3. 약점 분석: 추천 주제 집중 학습
4. 반복: 점수 향상 추적
```

### 학과/교수
```
1. 학생별 Storage 폴더 생성
2. 주기적 진도 확인 (Blob Storage 조회)
3. 반별 평균 점수 분석
4. 약한 주제 파악 → 수업 보강
```

### 기업 HR
```
1. 지원자별 면접 리포트 생성
2. 기술 역량 정량 평가
3. 면접관별 평가 일관성 확인
4. 합격자 vs 불합격자 패턴 분석
```

---

## ✅ 체크리스트

**Blob Storage 설정:**
- [ ] Azure Storage Account 생성
- [ ] Connection String 복사
- [ ] .env 파일에 설정
- [ ] `pip install azure-storage-blob`
- [ ] 시뮬레이터 실행 시 로그 확인

**PDF 리포트:**
- [ ] `pip install reportlab`
- [ ] 면접 모드에서 질문 답변
- [ ] 이름 입력
- [ ] PDF 생성 버튼 클릭
- [ ] 다운로드 확인

---

**이제 데이터가 자동으로 저장되고 전문적인 리포트를 생성할 수 있습니다! 🎉**
